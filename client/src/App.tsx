import Counter from "./components/Counter";

function App() {
  return (
    <div
      style={{
        width: "100vw",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
      }}
    >
      <Counter />
    </div>
  );
}

export default App;
